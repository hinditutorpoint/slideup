GOALS
- Video editor UI must be compact, responsive, and overflow-free
- Fix RenderFlex overflow issues permanently
- Editor must work on all device sizes
- Heavy tasks must run in background services
- App must be crash-free using try/catch and safe async handling
- Performance is highest priority

ANTI-HALLUCINATION & VERSION-SAFETY INSTRUCTION:
When generating code or architecture for this project, follow these rules strictly:

- Do NOT use outdated or deprecated APIs.
	- Always assume latest stable Flutter SDK and latest plugin versions.
	- Do NOT use old patterns unless explicitly requested.
	- Do NOT create unused variable.
- Never generate undefined variables, methods, or classes.
	- Every variable, method, and class must be declared or clearly referenced.
	- If something is assumed, clearly state the assumption.
- No fake or guessed APIs.
	- Do not invent plugin methods or FFmpeg commands.
	- If an API is uncertain, explain it in text before using it.
- Use explicit imports and correct file references.
	- Do not reference files, providers, or services that were not created earlier.
	- If a dependency is missing, mention it clearly.
- Version clarity is mandatory.
	- Mention Flutter version compatibility if relevant.
	- Mention plugin names exactly as used in pub.dev.
- Step-wise generation only.
	- Generate one module at a time.
	- Do not jump ahead to future modules.
- Fail safely.
	- Wrap async and heavy logic in try/catch.
	- Provide graceful error handling.
- No magic values.
	- Avoid hard-coded sizes, durations, or paths.
	- Use constants or configuration objects.
- Cross-check before final output.
	- Re-verify variables, references, and imports mentally before responding.
	- Output must compile logically.
- If something is ambiguous:
	- Ask a clarification OR
	- Provide a safe placeholder with TODO comment.

PROJECT STRUCTURE

lib/video_editor/
models/
providers/
services/
components/
tabs/
widgets/
utils/
video_editor_pro.dart

Main screen: video_editor_pro.dart
Accepts optional video file path and initializes editor

RESPONSIVE UI RULES

- No fixed heights unless unavoidable
- Always use LayoutBuilder for size awareness
- Use Expanded/Flexible correctly
- Use scroll only where required
- Timeline items must be virtualized
- Adaptive layouts:
  Mobile: Preview → Timeline → Tools
  Tablet: Split preview + timeline
  Desktop: Multi-panel layout

UNIFIED TIMELINE COMPONENT (CORE)

Single timeline for:
- Video
- Audio
- Image
- Text

Features:
- Drag items to change time
- Resize duration by dragging edges
- Layer reorder
- Multi-select items
- Group and ungroup items
- Marker support
- Zoomable timeline

Two sections:

A. Preview Component
- Video player
- Overlay objects
- Bottom-right: resize button
- Top-right: delete button
- Center: move button
- Gesture safe and overflow free

- TIMELINE PREVIEW FLOW
	- video_player plays main video
	- Timeline scrubbing seeks video_player
	- Text/image/audio overlays rendered in Flutter
	- No FFmpeg involved during preview
	- User sees smooth, real-time feedback

B. Timeline Component
- Horizontal scroll
- Zoom control
- Time ruler
- Layer rows

IMAGE & TEXT ANIMATION – OFFICIAL FEATURE ADDITION
Scope:
Image and Text items in unified timeline must support animations
Animations are non-destructive and editable
Same animation model used for preview and export
SUPPORTED ANIMATION TYPES

Entry animations:
Fade in, Slide in (left, right, top, bottom), Scale in, Rotate in, Exit animations:, Fade out, Slide out, Scale out, Rotate out, Loop / continuous:, Pulse, Bounce
Float, Shake, Transform animations:, Position (X, Y), Scale, Rotation, Opacity

ANIMATION CONTROLS (UI)
Each image/text item provides:, Animation enable/disable, Animation type selector, Duration, Delay, Easing curve, Loop toggle, Preview button, Animation controls live inside:Text tool, Image tool, Properties panel (right/side panel)
TIMELINE REPRESENTATION
Animation shown as sub-track or markers inside item, Entry animation at start, Exit animation at end, Loop animation spans full duration, Animations editable by dragging markers, PREVIEW IMPLEMENTATION (FAST), Preview rules:Flutter animations only, Use AnimatedBuilder / TweenAnimationBuilder, No FFmpeg during preview, Animations must run in real-time smoothly, Preview flow:video_player plays base video, Flutter renders image/text overlay, Flutter animation applies transforms

TOOLS TAB

Tabs included:
- Cut / Trim main video
- Color grading
- Text editor
- Music manager
- Image Library stock with pixabay api
- Videos Library stock with pixabay api
- AI tools
	- Generate Ai Image with api
	- Generate video 
- Merge audio/video
- Attach audio
- Extract from main video
- Export
- Recent exported list

Each tool works independently and returns output to unified timeline

BACKGROUND EXPORT SERVICE

Uses WorkManager

Provides:
- Background processing when app is closed
- Real-time progress notifications
- Completion notification
- Failure notification
- Job recovery (resume export)
- Cancel export support
- FFmpeg integration for overlays and filters

Export options:
- Presets: MP4, MOV, WebM, Audio only
- Custom: resolution, fps, bitrate, codec

EXPORTED / EXTRACTED MEDIA MANAGEMENT

Inside editor show list of:
- Extracted media
- Exported videos
- Rendered outputs

Actions available:
- Preview
- Rename
- Delete
- Share
- Re-edit (add back to timeline)

AUDIO EDITOR

Features:
- Echo
- Compressor
- Vocal enhancer
- Noise remover
- Vocal remover
- Sound remover
- Cut / Trim
- Merge audio

Audio workflow:
- Save edit
- Open edit
- Recent edits
- Export preset or custom
- Return result to unified timeline

IMAGE EDITOR

Rules:
- Image size inherits main video frame size
- Preset system ready for future updates

Features:
- Multi-layer editing
- Color mixing
- Text with presets
- Shapes
- Crop
- Paint and erase
- Resize with presets
- Background remover
- Auto enhancement
- Color grading palette

Image workflow:
- Save preset or custom
- Share
- Add back to unified timeline

VIDEO EDITOR

Features:
- Cut / Trim
- Resize
- Markers
- Chroma key
- Hue and saturation
- RGB color control
- Color grading presets

Video workflow:
- Save edit
- Open edit
- Recent edits
- Export preset or custom
- Return to unified timeline

STABILITY AND PERFORMANCE RULES

- All heavy operations inside try/catch
- Use isolates or background services
- No blocking UI thread
- Central error handling and logging
- Safe memory handling for FFmpeg
- Graceful failure UI

AI READY FUTURE SCOPE

- Auto cut detection
- AI captions
- Beat sync
- Smart color grading
- Object tracking
- AI background music

FINAL RESULT

- Compact and responsive video editor
- No RenderFlex overflow
- Background export with recovery
- Unified professional timeline
- Scalable Flutter architecture
- Production ready for premium video editing apps
