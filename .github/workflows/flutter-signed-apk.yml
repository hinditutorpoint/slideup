name: Flutter CI & Signed APK

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repo
      - uses: actions/checkout@v4

      # 2️⃣ Setup Flutter (exact revision match)
      - name: Setup Flutter
        uses: flutter-actions/setup-flutter@v4
        with:
          channel: stable
          version: latest

      - name: Clean previous builds
        run: |
          flutter clean
          rm -rf build
          rm -rf $HOME/.gradle/caches/
          rm -rf $HOME/.pub-cache/hosted/pub.dev/video_thumbnail-0.5.6
          rm -rf $HOME/.pub-cache/hosted/pub.dev/sherpa_onnx_android-*

      # 3️⃣ Verify Flutter
      - name: Flutter doctor
        run: flutter doctor -v

      # 4️⃣ Install dependencies
      - name: Flutter pub get
        run: flutter pub get

      # 5️⃣ Analyze code
      - name: Analyze project
        run: flutter analyze --no-fatal-warnings

      # 6️⃣ Run tests
      - name: Run Flutter tests
        run: flutter test

      # 7️⃣ Build signed APK (keystore step optional)
      - name: Build signed APK
        run: |
          flutter build apk --release --no-shrink --split-per-abi \
          --dart-define=API_BASE_URL=${{ secrets.API_BASE_URL }} \
          --dart-define=API_KEY=${{ secrets.API_KEY }} \
          --dart-define=PIXABAY_KEY=${{ secrets.PIXABAY_KEY }}
